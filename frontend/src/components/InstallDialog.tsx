// Temporary stub — Task 12 replaces this with the real install dialog
// (host select, overrides, root-consent, job stream).
export function InstallDialog(_: { slug: string; onClose: () => void }) {
  return null
}
